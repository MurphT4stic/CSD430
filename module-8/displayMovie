<!--Tabari Harvey, Module-8 Assignment Project Part 3, CSD 430-->
<!--This JSP file displays the details of the selected movie from the database.-->
<%@ page import="movies.MovieBean" %>
<html>
<head><title>Movie Details</title></head>
<body>
<h2>Movie Details</h2>
<%
    String movieId = request.getParameter("movie_id");
    try {
        MovieBean movie = MovieBean.getMovieById(Integer.parseInt(movieId));
        if (movie != null) {
%>
<table border="1">
    <thead>
        <tr>
            <th>ID</th><th>Title</th><th>Genre</th>
            <th>Year</th><th>Director</th><th>Rating</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><%= movie.getMovie_id() %></td>
            <td><%= movie.getTitle() %></td>
            <td><%= movie.getGenre() %></td>
            <td><%= movie.getRelease_year() %></td>
            <td><%= movie.getDirector() %></td>
            <td><%= movie.getRating() %></td>
        </tr>
    </tbody>
</table>
<%
        } else {
            out.println("<p>No movie found with ID " + movieId + "</p>");
        }
    } catch(Exception e) {
        out.println("<p>Error: " + e.getMessage() + "</p>");
    }
%>
</body>
</html>
